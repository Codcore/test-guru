<h1><%= @test.title %></h1>
<ul>
  <% @test.questions.each do |q| %>
    <li><%= q.body %>
      [<%= link_to "x", question_path(q), method: :delete, data: { confirm: "Are you sure?" } %>]
  <% end %>
</ul>
