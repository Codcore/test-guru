h1 #{@question.test.title} Question

p
  = @question.body

h2 Answers

= link_to 'New answer', new_admin_question_answer_path(@question)

ol
  = render partial: 'admin/answers/answer', collection: @question.answers

=> (link_to 'Edit', edit_admin_question_path(@question))
'|
=> (link_to 'Delete', admin_question_path, method: :delete, data: { confirm: 'Are you sure?' })
'|
=> link_to 'Back', admin_test_path(@question.test)